<%= form_tag item_path do %>
    <%= submit_tag "スカートを出力", class: "btn btn-primary btn-lg" %>
<% end %>

<h3>楽天</h3>
<% if @items.present? %>
    <% @items.each do |item| %>
        <%= link_to image_tag(item['mediumImageUrls'].first['imageUrl'], class: "img-thumbnail", border: '0'), item['itemUrl'], target: "_blank" %>
    <% end %>
<% else %>
    <p>ここに検索結果が20件出力されます。</p>
<% end %>